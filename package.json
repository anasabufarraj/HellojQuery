{
  "name": "hello_jquery",
  "version": "1.0.0",
  "description": "learning jQuery",
  "main": "index.html",
  "dependencies": {
    "gulp-cli": "^2.2.0",
    "lite-server": "^2.5.4"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "lite-server"
  }
}
